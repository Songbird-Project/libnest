# libnest

> [!IMPORTANT]
> `libnest` is early development, breaking changes may occur
> overnight with little to no forewarning.

The core of the Nest package manager.

## Usage

To use `libnest` in your project, run the following:
`zig fetch --save git+https://codeberg.org/Songbird-Project/libnest`

`libnest` tracks the latest tagged Zig release, currently: `0.16.0`

## Licensing

All items in the project are licensed under the BSD-3-Clause unless explicitly
stated within the file and here. All licences in use are provided under the
`LICENSES` directory, formatted as `LICENSE-[SPDX-Identifier]`, the BSD-3-Clause
license is symlinked to `LICENSE` at the top-level due to it being the projects
main license.

The following other licenses are in use:

|SPDX Identifier|Uses|
|-|-|
|GPL-2.0-or-later|makepkg|
